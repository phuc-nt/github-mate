# Dev Log — 2026-04-20

## phase-01: bootstrap (D1-only MVP)
- Next.js 16 + OpenNext + Cloudflare Workers scaffolded
- D1 `github_mate` created + migrated (local + remote): `profiles`, `match_history`, `top20_authors`, `sessions`
- KV `CACHE` namespace created
- 4 prod secrets uploaded via `wrangler secret bulk .dev.vars`
- Redesign from R2+Queues → D1-only (free-plan compatible); KG & vault inlined as TEXT columns
- `npm run preview` → HTTP 200 on `/` verified
- Initial commit pushed to `github.com:phuc-nt/github-mate`

## phase-02: github fetcher
- Added `lib/github/` module (9 files, all <200 LOC): types, profile-query, kv-cache, rate-limit, graphql-client, fetch-profile, fetch-commit-activity, fetch-readme, fetch-trending
- GraphQL query from research §A.1 (~425pts/profile budget; `repoCount`/`starCount` tunable)
- E2E test (`fetch-profile.test.ts`): 6 scenarios — cache miss, cache hit, invalid login, 404, rate-limit, skipCache — all green
- Fix: `mockResolvedValue` reused Response instance → body unusable; switched to `mockImplementation` for fresh Response per call
- Vitest added (`npm test`); typecheck clean
- Deferred: per-fetcher unit tests beyond the main orchestrator (covered indirectly; expand in phase-08)

## phase-03: knowledge graph builder
- Added `lib/kg/` module (10 files, all <200 LOC): types, community-assigner, framework-detector, node-factories, edge-factories, build-kg, emit-markdown, emit-graph-json, emit-obsidian-config, pack-vault-zip
- Node types: person/repo/language/topic/framework/org; edges: owns, uses, stars, tags, member_of, active_in (with confidence EXTRACTED/INFERRED/AMBIGUOUS + numeric score)
- Emits Obsidian-compatible vault: YAML frontmatter + wikilinks `[[Target]] - \`relation\` [CONF]`, networkx node-link graph.json, `.obsidian/graph.json` colorGroups
- fflate 0.8.2 for in-worker zip (workerd-compatible, ~10KB)
- E2E test (`build-kg.test.ts`): 5 scenarios — node/edge counts, frontmatter+wikilinks, dedup, graph.json shape, zip round-trip (<500KB) — all green on first run, no fixes needed
- Total test suite: 11/11 pass (183ms)

## phase-04: match scoring engine
- Added `lib/scoring/` module (8 files): types, cosine, tech-overlap (Jaccard on langs+frameworks), skill-heuristic (table-based classifier), complementary-skills (disjoint/partial/full overlap), interest-similarity (bge doc builder + cosine), kg-overlap (node/edge intersection), compute-match (orchestrator w/ weights 0.3/0.4/0.3)
- Also: `classifySkillsAI` path (Llama 3.3 70B w/ strict JSON parse, heuristic fallback on failure)
- E2E test: 17 scenarios (cosine edge cases, Jaccard, heuristic per skill family, complementary scoring, identical/disjoint matches, weight sum invariant, overlap subgraph) — all green
- Fix: complementaryScore logic — "identical" means shared==union, not shared==1 (single-tag full overlap edge case)
- Total test suite: 28/28 pass (168ms)

## phase-05: API routes (D1-only adapted)
- `lib/api/` helpers: parse-github-url (strict regex), hash-pair (sha256 sorted lowercase), d1-types (D1Like mockable), persist-profile (upsert w/ kg_json + vault_json inline), match-cache (read/write), oauth-github (authorize URL, state, code exchange, viewer fetch), session-store (AES-GCM token encryption via SESSION_SECRET, httpOnly cookie), match-orchestrator (parallel fetch → KG build → persist → score → cache)
- Route handlers (all `runtime='nodejs'`): POST `/api/match`, GET `/api/profile/[login]/kg`, GET `/api/profile/[login]/vault` (zip built in-worker from D1 vault_json), GET `/api/top20`, GET `/api/auth/github/login` + callback, GET `/api/auth/session`, POST `/api/auth/logout`
- OAuth: state stored in KV (10min TTL); session token encrypted per-request with AES-GCM before D1 insert
- E2E test: `runMatch` cache miss → full pipeline, then cache hit; OAuth authorize URL shape; hashPair order/case invariance; token encrypt round-trip w/ wrong-secret failure; cookie parse/clear
- Uniform error mapping: invalid URL/login → 400, not found → 404, GitHub rate-limit → 429, else 500
- Total test suite: 38/38 pass (222ms); typecheck clean

## phase-06: UI (landing, match, top 20)
- Updated `app/layout.tsx` with dark theme, header nav, SessionBadge
- `app/components/`: session-badge (Sign in / Sign out via /api/auth/*), url-input-form (controlled inputs, prefill via ?urlB=), score-card (big % + breakdown bars), graph-viewer (cytoscape dynamic import, nodes colored by type, overlap highlighted pink)
- `app/page.tsx`: landing with 2-URL form
- `app/match/page.tsx` + `match-client.tsx`: POSTs /api/match, cycles through loading stages, fetches both KGs, renders ScoreCard + GraphViewer + download buttons
- `app/top20/page.tsx`: server-rendered D1 read, "Match me →" prefill link
- `next build` green (all 10 routes compile); 38/38 tests still pass; typecheck clean
- Deferred: mobile responsive polish (phase-08)
