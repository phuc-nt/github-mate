# Dev Log — 2026-04-21

## phase-08: test & deploy
- `/ck:plan validate` Session 3 → skip Miniflare + TTL sweeper; add row-size guard + Playwright; ship with manual admin POST for cron (OpenNext scheduled() wiring deferred)
- `lib/kg/row-size-guard.test.ts`: 50-repo + 100-repo synthetic profiles → kg_json + vault_json each well under 900KB (D1 1MB cap has comfortable margin)
- Playwright: `playwright.config.ts` (chromium, `E2E_BASE_URL` override, auto-spawn `npm run dev` when unset) + `e2e/landing-smoke.spec.ts` (landing form placeholders, Top 20 heading, header nav links)
- `tsconfig.json` excludes `e2e/` + `playwright.config.ts` so typecheck passes without `@playwright/test` installed; listed as optional devDep + `npm run test:e2e[:install]` scripts
- `.github/workflows/deploy.yml`: `verify` job (checkout → setup-node 20 → npm ci → typecheck → test) on PR + main; `deploy` job (wrangler-action@v3) gated on main push → `d1 migrations apply DB --remote` then `npm run deploy`
- `docs/deployment-guide.md`: fresh-clone flow → create D1/KV → migrations (local + remote) → secrets (bulk `.dev.vars` or one-by-one) → dev vs preview vs deploy → CI secrets (`CLOUDFLARE_API_TOKEN`, `CLOUDFLARE_ACCOUNT_ID`) → admin POST Top 20 seed → custom domain → rollback + e2e
- `docs/codebase-summary.md`: module map for lib/github, lib/kg, lib/scoring, lib/api, lib/top20, app/ + D1 tables + KV usage + env bindings + deferred items
- Plan status: all 8 phases ✅; phase-08 phase file + plan.md both updated
- Final test suite: 41/41 pass (265ms); typecheck clean
- Launch punch list (user action): prod migrations, `wrangler secret put`, custom domain, initial `curl -X POST /api/admin/refresh-top20`

## launch: prod deploy
- `wrangler d1 migrations apply DB --remote` → no-ops (migrations already applied from phase-01)
- `ADMIN_TOKEN` generated (openssl rand -hex 32) + `wrangler secret put ADMIN_TOKEN` → 5 prod secrets total
- Deploy #1 failed: `invalid cron string: 0 0 * * 0` — `[triggers]` block in wrangler.toml registers cron, but worker has no `scheduled()` handler (OpenNext wiring deferred) → Cloudflare API rejects. Commented out `[triggers]` block; weekly refresh via admin POST per deployment-guide
- Deploy #2 success → `https://github-mate.phucnt.workers.dev`
- Seed Top 20 #1 returned `totalTrending: 0` — probed `github-trending-api.vercel.app` directly: HTTP 451 `DEPLOYMENT_DISABLED` (the single-source-of-failure risk from plan open Q#3 materialized)
- Swap `lib/github/fetch-trending.ts` → GitHub Search API (`q=pushed:>{30d-ago} stars:>5000 sort:stars-desc per_page=100`); threads existing `GITHUB_TOKEN` through `refreshTop20`; filters `owner.type === "User"` to skip orgs
- Updated refresh-top20 test mock (`github-trending-api` → `api.github.com/search/repositories`, new response shape with owner.type); added org filter assertion via 4th mock item
- Deploy #3 + seed #2 → `{"processed":[20 logins],"failed":[],"totalTrending":26}`; `/api/top20` returns full leaderboard (sindresorhus #1, torvalds #4, …)
- Still 41/41 tests + typecheck clean
